import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable static export for easy hosting on Cloudflare, Netlify, etc.
  // Remove the lines below if using dynamic routes / server actions
};

export default nextConfig;
